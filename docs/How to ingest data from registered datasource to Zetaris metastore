This document will show how we can ingest the data in our Zetaris metastore.
Steps to create a metastore and ingest data to metastore.

Step 1: Create a metastore namespace where data will be ingested.
CREATE NAMESPACE lightning.metastore.h2;

Step 2: Ingest schema to the Zetaris metastore
In below query h2 is the database name and PUBLIC is schema name

REGISTER or replace CATALOG PUBLIC
SOURCE lightning.datasource.rdbms.h2.PUBLIC
NAMESPACE lightning.metastore.h2;

Step 3: Ingest database to the Zetaris metastore
In below query h2 is the database name
REGISTER or replace CATALOG ALL_SCHEMA
SOURCE lightning.datasource.rdbms.h2
NAMESPACE lightning.metastore.h2;
